fastapi==0.104.1                  # keep if you need this exact, or upgrade
uvicorn==0.24.0                   # same
python-dotenv==1.0.0
aiohttp==3.9.1
httpx==0.25.2
authlib==1.3.0
pydantic>=2.5.3                   # bump slightly for safety
pydantic-settings==2.1.0
python-multipart==0.0.6
pytest==7.4.3                     # dev, ok
pytest-asyncio==0.21.1
pytest-httpx==0.26.0
requests==2.31.0

# Critical upgrades – remove old >= pins
langchain>=0.3.0                  # or pin ~0.3.20+ if stable
langgraph>=0.2.0                  # or ~0.2.20+ / latest
langchain-groq>=0.1.0             # newer Groq integration
langsmith>=0.1.100                # or >=0.2.0 – key for schemas fix